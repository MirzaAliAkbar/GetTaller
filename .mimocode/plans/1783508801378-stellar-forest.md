# Plan: Change Application ID to `com.grayonix.GetTaller`

## Scope
Replace all application/bundle identifiers across Android, iOS, macOS, Linux, Windows, web, and Dart with `com.grayonix.GetTaller`. Leave `pubspec.yaml` name (`gettaller_app`) and keystore config unchanged.

## Changes

### 1. Android
- **`android/app/build.gradle`** — Change `applicationId` and `namespace` from `com.gettaller.gettaller_app` to `com.grayonix.GetTaller`
- **Move Kotlin source** — Move `android/app/src/main/kotlin/com/gettaller/gettaller_app/` to `android/app/src/main/kotlin/com/grayonix/GetTaller/`
- **`MainActivity.kt`** — Update `package` declaration to `com.grayonix.GetTaller`

### 2. iOS
- **`ios/Runner.xcodeproj/project.pbxproj`** — Replace all 6 occurrences of `com.gettaller.gettallerApp` with `com.grayonix.GetTaller`
- **`ios/Runner/Info.plist`** — Update `CFBundleDisplayName` from `Gettaller App` to `GetTaller`

### 3. macOS
- **`macos/Runner/Configs/AppInfo.xcconfig`** — Change `PRODUCT_BUNDLE_IDENTIFIER` from `com.gettaller.gettallerApp` to `com.grayonix.GetTaller`
- **`macos/Runner.xcodeproj/project.pbxproj`** — Replace all `com.gettaller.gettallerApp.RunnerTests` with `com.grayonix.GetTaller.RunnerTests`
- **`macos/Runner/Configs/AppInfo.xcconfig`** — Update `PRODUCT_NAME` and `PRODUCT_COPYRIGHT`

### 4. Dart
- **`lib/core/utils/constants.dart`** — Change `packageName` from `com.gettaller.app` to `com.grayonix.GetTaller`

### 5. Linux
- **`linux/CMakeLists.txt`** — Change `APPLICATION_ID` from `com.gettaller.gettaller_app` to `com.grayonix.GetTaller`
- **`linux/my_application.cc`** — Update window title from `gettaller_app` to `GetTaller`

### 6. Windows
- **`windows/CMakeLists.txt`** — Update `project` name if needed
- **`windows/runner/Runner.rc`** — Update `CompanyName` to `com.grayonix`, `FileDescription`/`ProductName` to `GetTaller`, copyright line
- **`windows/runner/main.cpp`** — Update window title to `GetTaller`

### 7. Web
- **`web/manifest.json`** — Update `name` and `short_name` to `GetTaller`
- **`web/index.html`** — Update `<title>`, `apple-mobile-web-app-title`, and `og:title` to `GetTaller`

### 8. Documentation
- **`FIREBASE_SETUP.md`** — Update package name references
- **`README.md`** — Update project title

## Files Modified (18)
1. `android/app/build.gradle`
2. `android/app/src/main/kotlin/com/grayonix/GetTaller/MainActivity.kt` (moved + edited)
3. `ios/Runner.xcodeproj/project.pbxproj`
4. `ios/Runner/Info.plist`
5. `macos/Runner/Configs/AppInfo.xcconfig`
6. `macos/Runner.xcodeproj/project.pbxproj`
7. `lib/core/utils/constants.dart`
8. `linux/CMakeLists.txt`
9. `linux/my_application.cc`
10. `windows/CMakeLists.txt`
11. `windows/runner/Runner.rc`
12. `windows/runner/main.cpp`
13. `web/manifest.json`
14. `web/index.html`
15. `FIREBASE_SETUP.md`
16. `README.md`

## Not Changed (by user decision)
- `pubspec.yaml` name stays `gettaller_app`
- Keystore alias/password stays `gettaller`/`gettaller123`
- Test imports stay `package:gettaller_app/...` (since pubspec name unchanged)

## Verification
1. `flutter analyze` — no errors from changed identifiers
2. Grep for `com.gettaller` to confirm zero remaining references
3. Check that old Kotlin directory `com/gettaller/gettaller_app/` is removed
